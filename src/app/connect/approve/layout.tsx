import type { Metadata } from "next";

export const metadata: Metadata = { title: "Connect" };

export default function ConnectApproveLayout({ children }: { children: React.ReactNode }) {
  return children;
}
