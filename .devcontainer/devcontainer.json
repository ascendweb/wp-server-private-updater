{
  "name": "WP Private Updater",
  "dockerComposeFile": "docker-compose.yaml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "forwardPorts": [
    3000,
    5432
  ],
  "postCreateCommand": "bash -lc 'repo=$(ls -d /workspaces/* 2>/dev/null | head -n 1); if [ -z \"$repo\" ]; then echo \"No repo found under /workspaces\"; exit 1; fi; cd \"$repo\"; bash ./.devcontainer/postCreateCommand.sh'",
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.codeActionsOnSave": [
          "source.fixAll.eslint"
        ],
        "editor.tabSize": 2,
        "editor.detectIndentation": false,
        "files.eol": "\n",
        "terminal.integrated.cwd": "/",
        "search.exclude": {
          "**/.git": true,
          "**/node_modules": true,
          "**/package-lock.json": true
        }
      },
      "extensions": [
        "Prisma.prisma",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "bradlc.vscode-tailwindcss"
      ]
    }
  }
}
